"use client";
/* eslint-disable react-hooks/set-state-in-effect, react-hooks/exhaustive-deps */

import { useEffect, useState } from "react";

type Request = <T>(url: string, init?: RequestInit) => Promise<T>;
type Report = { current: { metrics: Record<string, number>; daily: { date: string; revenue: number; refunds: number }[]; salons: { salonId: string; salonName: string; code: string; revenue: number; refunds: number }[] }; comparison: { metrics: Record<string, number> } | null };
const money = (value: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(value);
const iso = (date: Date) => date.toISOString().slice(0, 10);

export function ReportsConsole({ request }: { request: Request }) {
  const today = iso(new Date()); const firstDay = iso(new Date(new Date().getFullYear(), new Date().getMonth(), 1));
  const [from, setFrom] = useState(firstDay); const [to, setTo] = useState(today); const [compareFrom, setCompareFrom] = useState(""); const [compareTo, setCompareTo] = useState("");
  const [report, setReport] = useState<Report | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState("");
  const load = async () => { setLoading(true); setError(""); try { const query = new URLSearchParams({ from, to, ...(compareFrom ? { compareFrom } : {}), ...(compareTo ? { compareTo } : {}) }); setReport(await request<Report>(`/api/platform/reports/financial?${query}`)); } catch (cause) { setError(cause instanceof Error ? cause.message : "Could not load report."); } finally { setLoading(false); } };
  useEffect(() => { void load(); }, []);
  const csv = () => { window.location.assign(`/api/platform/reports/financial.csv?${new URLSearchParams({ from, to })}`); };
  const metrics = report?.current.metrics; const comparison = report?.comparison?.metrics;
  const delta = (key: string) => comparison ? (metrics?.[key] ?? 0) - (comparison[key] ?? 0) : null;
  const cards = metrics ? [["Revenue", "revenue"], ["GST collected", "gst"], ["Refunds", "refunds"], ["Expenses", "expenses"], ["Payouts", "payouts"], ["Profit / loss", "profitLoss"], ["Platform billing", "platformBillingRevenue"]] as const : [];
  const maxRevenue = Math.max(...(report?.current.daily.map((item) => item.revenue) ?? [0]), 1);
  return <section className="space-y-5 print:p-0"><form className="flex flex-wrap items-end gap-3 rounded-2xl border border-zinc-200 bg-white p-4 shadow-sm print:hidden" onSubmit={(event) => { event.preventDefault(); void load(); }}><DateInput label="From" value={from} setValue={setFrom}/><DateInput label="To" value={to} setValue={setTo}/><DateInput label="Compare from" value={compareFrom} setValue={setCompareFrom}/><DateInput label="Compare to" value={compareTo} setValue={setCompareTo}/><button className="rounded-xl bg-zinc-900 px-4 py-2 text-sm font-semibold text-white">Apply</button><button type="button" onClick={csv} className="rounded-xl border border-zinc-300 px-4 py-2 text-sm font-semibold">CSV</button><button type="button" onClick={() => window.print()} className="rounded-xl border border-zinc-300 px-4 py-2 text-sm font-semibold">Print / Save PDF</button></form>{error && <p className="rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<div className="hidden print:block"><h2 className="text-2xl font-bold">DropXCutz financial report</h2><p>{from} to {to}</p></div>{loading ? <div className="h-64 animate-pulse rounded-2xl bg-zinc-100"/> : metrics && <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, key]) => <article key={key} className="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-wide text-zinc-400">{label}</p><p className={`mt-2 text-2xl font-bold ${key === "profitLoss" && metrics[key] < 0 ? "text-rose-700" : "text-zinc-950"}`}>{money(metrics[key])}</p>{delta(key) !== null && <p className={`mt-2 text-xs font-semibold ${delta(key)! >= 0 ? "text-emerald-700" : "text-rose-700"}`}>{delta(key)! >= 0 ? "+" : ""}{money(delta(key)!)} vs comparison</p>}</article>)}</div><div className="grid gap-5 xl:grid-cols-[1.2fr_1fr]"><article className="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Daily paid revenue</h2><div className="mt-5 flex h-56 items-end gap-1">{report.current.daily.map((item) => <div key={item.date} title={`${item.date}: ${money(item.revenue)}`} className="flex-1 rounded-t bg-zinc-900" style={{ height: `${Math.max((item.revenue / maxRevenue) * 100, 1)}%` }}/>)}</div><div className="mt-3 flex justify-between text-xs text-zinc-500"><span>{from}</span><span>{to}</span></div></article><article className="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Tax & settlement summary</h2><dl className="mt-4 space-y-3 text-sm"><Row label="Paid salon invoices" value={String(metrics.paidInvoiceCount)}/><Row label="Refunded invoices" value={String(metrics.refundedInvoiceCount)}/><Row label="Platform billing invoices" value={String(metrics.platformBillingInvoiceCount)}/><Row label="Platform GST" value={money(metrics.platformBillingGst)}/></dl></article></div><article className="rounded-2xl border border-zinc-200 bg-white shadow-sm"><div className="border-b p-5"><h2 className="font-bold">Salon revenue and refund breakdown</h2></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-zinc-50 text-xs uppercase text-zinc-500"><tr><th className="p-4">Salon</th><th className="p-4">Code</th><th className="p-4 text-right">Paid revenue</th><th className="p-4 text-right">Refunds</th></tr></thead><tbody>{report.current.salons.map((salon) => <tr key={salon.salonId} className="border-t"><td className="p-4 font-semibold">{salon.salonName}</td><td className="p-4 font-mono text-xs">{salon.code}</td><td className="p-4 text-right">{money(salon.revenue)}</td><td className="p-4 text-right">{money(salon.refunds)}</td></tr>)}</tbody></table></div></article></>}</section>;
}

function DateInput({ label, value, setValue }: { label: string; value: string; setValue: (value: string) => void }) { return <label className="text-sm font-semibold">{label}<input type="date" value={value} onChange={(event) => setValue(event.target.value)} className="mt-1 block rounded-lg border p-2 font-normal"/></label>; }
function Row({ label, value }: { label: string; value: string }) { return <div className="flex justify-between gap-4"><dt className="text-zinc-500">{label}</dt><dd className="font-semibold">{value}</dd></div>; }
