import type{Request,Response}from"express";import{Router}from"express";import{z}from"zod";import{prisma}from"../../config/prisma";import{ok,salonId}from"../../controllers/http.controller";import{requireSalonAdmin}from"../../middleware/session.middleware";const query=z.object({page:z.coerce.number().int().min(1).default(1),limit:z.coerce.number().int().min(1).max(100).default(20),search:z.string().trim().max(100).optional(),entity:z.string().trim().max(80).optional(),action:z.string().trim().max(100).optional(),userId:z.string().trim().optional(),sortOrder:z.enum(["asc","desc"]).default("desc")});export const salonAuditLogRouter=Router();salonAuditLogRouter.get("/",requireSalonAdmin,async(req:Request,res:Response)=>{const q=query.parse(req.query);const where={salonId:salonId(res),...(q.entity&&{entity:q.entity}),...(q.action&&{action:q.action}),...(q.userId&&{userId:q.userId}),...(q.search&&{OR:[{entity:{contains:q.search,mode:"insensitive" as const}},{action:{contains:q.search,mode:"insensitive" as const}},{entityId:{contains:q.search,mode:"insensitive" as const}}]})};const[data,total]=await Promise.all([prisma.salonAuditLog.findMany({where,include:{user:{select:{id:true,name:true,email:true}}},orderBy:{createdAt:q.sortOrder},skip:(q.page-1)*q.limit,take:q.limit}),prisma.salonAuditLog.count({where})]);ok(res,{data,pagination:{page:q.page,limit:q.limit,total,totalPages:Math.ceil(total/q.limit)}});});
