import {z}from"zod";export const membershipPlanInput=z.object({name:z.string().trim().min(1).max(160),duration:z.coerce.number().int().positive(),price:z.coerce.number().min(0),discountPercentage:z.coerce.number().min(0).max(100).default(0),loyaltyMultiplier:z.coerce.number().min(0).max(100).default(1),isActive:z.boolean().default(true)});export const membershipAssignInput=z.object({customerId:z.string().min(1),membershipPlanId:z.string().min(1)});
